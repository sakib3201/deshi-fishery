meta {
  name: Delete Stock Release
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/{{apiVersion}}/stock-releases/{{stockReleaseId}}
  body: none
  auth: bearer
}

headers {
  X-Farm-ID: {{farmId}}
}

auth:bearer {
  token: {{accessToken}}
}
