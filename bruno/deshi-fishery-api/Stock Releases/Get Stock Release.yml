meta {
  name: Get Stock Release
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/stock-releases/{{stockReleaseId}}
  body: none
  auth: bearer
}

headers {
  X-Farm-ID: {{farmId}}
}

auth:bearer {
  token: {{accessToken}}
}
