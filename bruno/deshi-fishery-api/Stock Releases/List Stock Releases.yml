meta {
  name: List Stock Releases
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/stock-releases
  body: none
  auth: bearer
}

headers {
  X-Farm-ID: {{farmId}}
}

auth:bearer {
  token: {{accessToken}}
}
