meta {
  name: Get Pond
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/ponds/{{pondId}}
  body: none
  auth: bearer
}

headers {
  X-Farm-ID: {{farmId}}
}

auth:bearer {
  token: {{accessToken}}
}
